<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/menu_item_height"
    android:orientation="vertical"
    android:stateListAnimator="@anim/raise_animation" >

    <View
        android:layout_width="wrap_content"
        android:layout_height="0.5dp" />

    <!-- Menu Item -->

    <LinearLayout
        android:id="@+id/item_menu_wrapper"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingLeft="10dp"
        android:paddingRight="10dp" >

        <!-- <com.nexlesoft.tenwordsaday_chiforeng.widget.RoundedImageView -->
        <!-- android:id="@+id/menu_item_iv" -->
        <!-- android:layout_width="50dp" -->
        <!-- android:layout_height="50dp" -->
        <!-- android:contentDescription="@null" -->
        <!-- android:scaleType="centerInside" /> -->

        <TextView
            android:id="@+id/menu_item_text"
            android:layout_width="0dip"
            android:layout_height="fill_parent"
            android:layout_marginLeft="5dp"
            android:layout_weight="1"
            android:gravity="left|center_vertical"
            android:singleLine="true"
            android:textColor="@color/white"
            android:textSize="15sp" />

        <ImageView
            android:id="@+id/menu_item_drop"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@null"
            android:visibility="gone" />

        <com.bigcatfamily.myringtones.widget.BadgeView
            android:id="@+id/menu_item_counter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone" />
    </LinearLayout>

</LinearLayout>